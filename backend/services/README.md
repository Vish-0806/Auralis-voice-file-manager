# Gateway Services API

## Purpose of this Module
Communication interface between the system and UI clients.

## Future Responsibility
Declaring REST routes, exposing SSE reasoning loops, managing WebSockets audio streams.

## What Should Belong Here
- FastAPI routers, WebSockets connection managers, SSE emitter classes.

## What Should NOT Belong Here
- Fuzzy file search tools, prompt string compilations, direct Win32 API calls.
